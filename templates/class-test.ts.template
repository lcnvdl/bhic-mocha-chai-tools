import { expect } from "chai";
import {{ classNameForImport }}  from "{{filename}}";

let instance: {{ className }} = null;

describe("{{ className }}", () => {
  beforeEach(() => {
    instance = new {{ className }}();
  });

  describe("#constructor", () => {
    it("should work fine", () => {
      expect(instance).to.be.ok;
    });
  });
});
